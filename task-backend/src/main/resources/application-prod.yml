spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sql12.freesqldatabase.com/sql12713933
    username: sql12713933
    password: mIiCTVQGH5

springdoc:
  openapi:
    url: https://jobapp-api.com
    description: Server URL in Production environment

jwt:
  access-token:
    expires: 3600000
  refresh-token:
    expires: 604800000
